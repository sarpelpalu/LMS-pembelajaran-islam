rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Aturan dasar untuk semua dokumen
    match /{document=**} {
      // Izinkan akses (baca dan tulis) HANYA JIKA pengguna sudah login
      allow read, write: if request.auth != null;
    }
  }
}
